[pytest]
testpaths = tests/live tests/live_storage
addopts = -ra
markers =
    live: live integration test (registered here too so it never warns when security-tests-m8's plugin is disabled, e.g. -p no:security_tests_m8 for tests/live_storage)
