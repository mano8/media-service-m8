fastapi-m8[db,postgres,mysql]>=2.1.0,<3.0.0
uvicorn>=0.46.0

alembic>=1.18.4

pydantic>=2.10.6
pydantic_settings>=2.7.1

tenacity>=9.1.4

email-validator>=2.3.0

bcrypt>=5.0.0

python-multipart>=0.0.28
itsdangerous>=2.2.0

# important: arq requires redis <6.0.0
redis>=5.3.1,<6.0.0
minio>=7.2.18

# Shared media SDK: object-storage client + producer↔consumer job contracts
# (incl. OutboxEventPayload — the Phase 16 outbound webhook contract, and the
# 0.4.0 chunked ObjectStorage.stream_object primitive used by streaming SHA-256
# verification, plan item 6.x.3)
media-sdk-m8>=0.4.0

# Background-job producer: enqueues scan + variant jobs for media-worker-m8
arq>=0.28.0

# Outbound webhook delivery: the maintenance worker POSTs signed outbox events
httpx>=0.27.0

python-slugify>=8.0.4

filetype>=1.2.0

jinja2>=3.1.6

google-auth>=2.52.0

# Optional observability (required when METRICS_ENABLED=true)
prometheus-client>=0.25.0

# DB drivers (ALL included → runtime choice)
asyncpg>=0.31.0
